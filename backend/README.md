# Backend starter

**Planning placeholder, not a running API.** Scaffold NestJS after confirming identity provider and data residency. Implement token verification, user/group authorization, versioned endpoints, request validation, rate limits and WebSocket audience checks before handling personal data.
